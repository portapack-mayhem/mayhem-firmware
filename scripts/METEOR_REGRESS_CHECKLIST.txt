Meteor-specific regression checks (after changing Meteor / external-app / IPC code)

  [ ] firmware/tools/external_app_info.py: external_apps_address_start/end and maximum_application_size
      stay consistent with firmware/application/external/external.ld (see comments in both files).
  [ ] firmware/baseband/CMakeLists.txt: DeclareTargets(PMLR meteor_lrpt_rx) still present; USE_OPT stays
      size-friendly (-Os) unless you have measured proof and SPI headroom.
  [ ] firmware/application/external/external.cmake: meteor_lrpt_rx external target matches the PMLR image.
  [ ] firmware/common/portapack_shared_memory.hpp: MeteorLrptIpc / MeteorLrptG4Ipc layout and consumers
      on M0 and M4 stay in agreement (misalignment tends to show as post-boot crashes).
  [ ] Re-run export / full firmware link: .ppma must not exceed the Python/linker limits.
